<nav class="sticky top-0 z-50 w-full bg-white/80 backdrop-blur-md border-b border-slate-200 shadow-sm transition-all duration-300">

    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">

        <div class="flex items-center justify-between h-18">

            <!-- Logo -->
            <a href="/" class="flex items-center gap-3 group">
    <div class="rounded-lg">
        <img src="/images/logo.png"
             class="w-12 h-12"
             alt="WarmupExam-logo">
    </div>

        <span class="font-heading text-2xl md:text-[30px] lg:text-[32px]
               font-extrabold text-indigo-600
               tracking-[-0.8px] leading-none">
        WarmupExam
    </span>
</a>

            <!-- Desktop Menu -->
            <div class="hidden md:flex items-center gap-8 font-medium">
                <% if (currentPath !== '/') { %>
<a href="/" class="nav-link hover:text-indigo-700 transition">Home</a> 
<% } %>
<a href="/alltests" class="nav-link hover:text-indigo-700 transition">Test Series</a>

<!-- Skill Tests dropdown -->
<div class="relative group">
    <button class="nav-link hover:text-indigo-700 transition flex items-center gap-1">
        Skill Tests
        <i class="fa-solid fa-chevron-down text-xs transition-transform group-hover:rotate-180"></i>
    </button>
    <div class="absolute left-0 top-full pt-2 w-56 hidden group-hover:block z-50">
        <div class="bg-white border border-slate-200 rounded-xl shadow-lg py-2">
            <a href="/skill-tests/typing-test" class="flex items-center gap-2 px-4 py-2 text-sm text-slate-700 hover:bg-indigo-50 hover:text-indigo-700 transition">
    <i class="fa-solid fa-keyboard w-4 text-slate-400"></i> Typing Test
</a>
            <a href="/skill-tests/data-entry-test" class="flex items-center gap-2 px-4 py-2 text-sm text-slate-700 hover:bg-indigo-50 hover:text-indigo-700 transition">
                <i class="fa-solid fa-table w-4 text-slate-400"></i> Data Entry Test
            </a>
            <a href="/skill-tests/calculation-test" class="flex items-center gap-2 px-4 py-2 text-sm text-slate-700 hover:bg-indigo-50 hover:text-indigo-700 transition">
                <i class="fa-solid fa-calculator w-4 text-slate-400"></i> Calculation Test
            </a>
        </div>
    </div>
</div>

                <a href="/ebooks" class="nav-link hover:text-indigo-700 transition">E-Books</a> 
                <a href="/aboutUs" class="nav-link hover:text-indigo-700 transition">About Us</a>
                <a href="/contactUs" class="nav-link hover:text-indigo-700 transition">Contact</a>
            </div>

            <!-- Desktop Buttons -->
            <div class="hidden md:flex items-center gap-3">

                <% if (currentUser) { %>

                    <%
                        const displayName = isOwner ? "Owner" : (currentUser.name || currentUser.email || "User");
                        const initial = (isOwner ? currentUser.email : (currentUser.name || currentUser.email || "U")).charAt(0).toUpperCase();
                        const hasBatchAccess = !isOwner && currentUser.enrolledListings && currentUser.enrolledListings.length > 0;
                        const targetSlug = hasBatchAccess
                            ? (currentUser.lastAccessedBatch ? currentUser.lastAccessedBatch.slug : currentUser.enrolledListings[0].listing.slug)
                            : null;
                    %>

                    <% if (isOwner) { %>
                        <a href="/owner-dashboard"
                            class="px-5 py-2 rounded-lg bg-indigo-600 text-white font-medium hover:bg-indigo-700 transition">
                            Go to Dashboard
                        </a>
                        <a href="/tests/new"
                            class="px-5 py-2 rounded-lg bg-orange-500 text-white hover:bg-orange-600 transition">
                            Upload Test Series
                        </a>
                    <% } else if (hasBatchAccess) { %>
                        <a href="/series/<%= targetSlug %>"
                            class="px-5 py-2 rounded-lg bg-indigo-600 text-white font-medium hover:bg-indigo-700 transition">
                            Go to Dashboard
                        </a>
                    <% } %>
 
                    <!-- Avatar + Username + hover dropdown -->

                    <!-- Avatar + Username + hover dropdown -->
<div class="relative group">
    <a href="<%= isOwner ? '/owner-dashboard' : '/profile' %>"
        class="flex items-center gap-2 pr-3 pl-1 py-1 rounded-full hover:bg-slate-100 transition">
        <span class="w-9 h-9 rounded-full bg-indigo-100 text-indigo-700 font-bold flex items-center justify-center overflow-hidden border-2 border-indigo-200 shrink-0">
            <% if (!isOwner && currentUser.avatar) { %>
                <img src="<%= currentUser.avatar %>" alt="<%= displayName %>" class="w-full h-full object-cover">
            <% } else { %>
                <span><%= initial %></span>
            <% } %>
        </span>

        <% if (!isOwner && currentUser.username) { %>
            <span class="flex items-center gap-1 text-sm font-semibold text-slate-800 max-w-[120px] truncate">
                @<%= currentUser.username %>
                <% if (currentUser.isVerified) { %>
                    <i class="fa-solid fa-circle-check text-indigo-600 text-xs shrink-0" title="Verified"></i>
                <% } %>
            </span>
        <% } else if (isOwner) { %>
            <span class="text-sm font-semibold text-slate-800">Owner</span>
        <% } %>
    </a>

    <!-- Dropdown -->
    <div class="absolute right-0 top-full pt-2 w-48 hidden group-hover:block z-50">
        <div class="bg-white border border-slate-200 rounded-xl shadow-lg py-2">
            <p class="px-4 py-2 text-sm font-semibold text-slate-800 border-b border-slate-100 truncate">
                <%= displayName %>
            </p>
            <% if (!isOwner) { %>
                <a href="/profile" class="flex items-center gap-2 px-4 py-2 text-sm text-slate-700 hover:bg-slate-50 transition">
                    <i class="fa-solid fa-user w-4 text-slate-400"></i> Open Profile
                </a>
            <% } %>
            <form action="/logout" method="POST" class="logout-form">
                 <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                <button type="submit" class="w-full flex items-center gap-2 px-4 py-2 text-sm text-red-600 hover:bg-red-50 transition text-left">
                    <i class="fa-solid fa-right-from-bracket w-4"></i> Logout
                </button>
            </form>
        </div>
    </div>
</div>

 

                <% } else { %>

                    <button data-action="open-auth" data-mode="login"
                        class="bg-white text-slate-900 px-5 py-2 rounded-lg font-medium hover:bg-slate-100 transition">
                        Login
                    </button>

                    <button data-action="open-auth" data-mode="register"
                        class="bg-indigo-600 text-white px-5 py-2 rounded-lg font-medium hover:bg-indigo-700 transition">
                        Sign Up
                    </button>

                <% } %>

            </div>

            <!-- Mobile Menu Button -->
            <button id="menuBtn" class="md:hidden text-3xl text-slate-700">
                <i class="fas fa-bars"></i>
            </button>

        </div>

    </div>

</nav>

<!-- Overlay -->
<div id="overlay" class="fixed inset-0 bg-black/40 hidden hidden z-[55]"></div>
 

<!-- Mobile Sidebar -->
<div id="mobileMenu"
     class="fixed top-0 right-0 w-80 max-w-[85vw] h-full bg-white/90 backdrop-blur-2xl shadow-2xl z-[60] transform translate-x-full transition-transform duration-300 ease-out md:hidden border-l border-slate-200/60">

    <div class="flex justify-between items-center px-6 py-5 border-b border-slate-100">
        <h2 class="text-lg font-bold text-slate-800 flex items-center gap-2">
            Menu
        </h2>
        <button id="closeBtn" class="w-9 h-9 flex items-center justify-center rounded-full text-slate-500 hover:bg-slate-100 hover:text-slate-800 transition">
            <i class="fas fa-times text-lg"></i>
        </button>
    </div>

    <div class="flex flex-col h-[calc(100%-73px)] overflow-y-auto">

        <!-- Nav Links -->
        <div class="flex flex-col p-6 gap-1">
            <% if (currentPath !== '/') { %>
<a href="/" class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-700 font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
    <i class="fa-solid fa-house w-4 text-slate-400"></i> Home
</a>
<% } %>
<a href="/alltests" class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-700 font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
    <i class="fa-solid fa-clipboard-list w-4 text-slate-400"></i> Test Series
</a>

<!-- Skill Tests accordion -->
<button type="button" id="skillTestsToggle"
    class="w-full flex items-center justify-between px-3 py-2.5 rounded-xl text-slate-700 font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
    <span class="flex items-center gap-3">
        <i class="fa-solid fa-graduation-cap w-4 text-slate-400"></i> Skill Tests
    </span>
    <i class="fa-solid fa-chevron-down text-xs transition-transform" id="skillTestsChevron"></i>
</button>
<div id="skillTestsPanel" class="hidden flex-col pl-9 gap-1">
    <a href="/skill-tests/typing-test" class="flex items-center gap-2 px-3 py-2 rounded-lg text-sm text-slate-600 hover:bg-indigo-50 hover:text-indigo-700 transition">Typing Test</a>
    <a href="/skill-tests/data-entry-test" class="flex items-center gap-2 px-3 py-2 rounded-lg text-sm text-slate-600 hover:bg-indigo-50 hover:text-indigo-700 transition">Data Entry Test</a>
    <a href="/skill-tests/calculation-test" class="flex items-center gap-2 px-3 py-2 rounded-lg text-sm text-slate-600 hover:bg-indigo-50 hover:text-indigo-700 transition">Calculation Test</a>
</div>

            <a href="/ebooks" class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-700 font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
              <i class="fa-solid fa-book w-4 text-slate-400"></i> E-Books
            </a>
            
            <a href="/aboutUs" class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-700 font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
                <i class="fa-solid fa-circle-info w-4 text-slate-400"></i> About Us
            </a>
            <a href="/contactUs" class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-slate-700 font-medium hover:bg-indigo-50 hover:text-indigo-700 transition">
                <i class="fa-solid fa-envelope w-4 text-slate-400"></i> Contact
            </a>

            <% if (isOwner) { %>
                <a href="/tests/new" class="flex items-center gap-3 px-3 py-2.5 rounded-xl text-orange-600 font-medium hover:bg-orange-50 transition">
                    <i class="fa-solid fa-upload w-4"></i> Upload Test Series
                </a>
            <% } %>
        </div>

        <hr class="border-slate-100 mx-6">

        <% if (currentUser) { %>

            <%
               const mDisplayName = isOwner ? "Owner" : (currentUser.name || currentUser.email || "User");
               const mInitial = (isOwner ? currentUser.email : (currentUser.name || currentUser.email || "U")).charAt(0).toUpperCase();
               const mHasBatchAccess = !isOwner && currentUser.enrolledListings && currentUser.enrolledListings.length > 0;
               const mTargetSlug = mHasBatchAccess
                   ? (currentUser.lastAccessedBatch ? currentUser.lastAccessedBatch.slug : currentUser.enrolledListings[0].listing.slug)
                   : null;
            %>

            <div class="flex flex-col gap-4 p-6 mt-auto">

                <!-- Mini profile card -->
                <a href="/profile" class="flex items-center gap-3 bg-slate-50 rounded-2xl p-3 border border-slate-100 hover:bg-slate-100 transition">
    <div class="w-11 h-11 rounded-full bg-gradient-to-br from-indigo-500 to-indigo-700 text-white font-bold flex items-center justify-center overflow-hidden shrink-0 shadow-sm">
        <% if (!isOwner && currentUser.avatar) { %>
            <img src="<%= currentUser.avatar %>" alt="<%= mDisplayName %>" class="w-full h-full object-cover">
        <% } else { %>
            <span><%= mInitial %></span>
        <% } %>
    </div>
    <div class="min-w-0">
        <p class="text-sm font-semibold text-slate-800 truncate"><%= mDisplayName %></p>
        <p class="text-xs text-slate-500 truncate"><%= isOwner ? "Owner Account" : "Student" %></p>
    </div>
</a>

                <% if (isOwner) { %>
                    <a href="/owner-dashboard"
                        class="w-full flex items-center justify-center gap-2 bg-indigo-600 text-white px-5 py-3 rounded-xl font-semibold hover:bg-indigo-700 transition shadow-sm shadow-indigo-200">
                        <i class="fa-solid fa-gauge"></i> Go to Dashboard
                    </a>
                <% } else if (mHasBatchAccess) { %>
                    <a href="/series/<%= mTargetSlug %>"
                        class="w-full flex items-center justify-center gap-2 bg-indigo-600 text-white px-5 py-3 rounded-xl font-semibold hover:bg-indigo-700 transition shadow-sm shadow-indigo-200">
                        <i class="fa-solid fa-gauge"></i> Go to Dashboard
                    </a>
                <% } %>

                <form action="/logout" method="POST" class="logout-form">
                     <input type="hidden" name="_csrf" value="<%= csrfToken %>">
                    <button type="submit"
                        class="w-full flex items-center justify-center gap-2 bg-red-50 text-red-600 px-5 py-3 rounded-xl font-semibold hover:bg-red-100 transition">
                        <i class="fa-solid fa-right-from-bracket"></i> Logout
                    </button>
                </form>
            </div>

        <% } else { %>

            <div class="flex flex-col gap-3 p-6 mt-auto">
                <button data-action="open-auth" data-mode="login"
                    class="w-full bg-white text-slate-900 border border-slate-200 px-5 py-3 rounded-xl font-semibold hover:bg-slate-50 transition">
                    Login
                </button>

                <button data-action="open-auth" data-mode="register"
                    class="w-full bg-indigo-600 text-white px-5 py-3 rounded-xl font-semibold hover:bg-indigo-700 transition shadow-sm shadow-indigo-200">
                    Sign Up
                </button>
            </div>

        <% } %>

    </div>

</div>





